/**
 * 笔记模块的前端域常量
 *
 * 这里只放「前端多处要读、且必须与后端保持一致」的值，集中一处避免散成多个字面量。
 */

/**
 * 笔记正文上限，与后端 `SgjNoteServiceImpl.CONTENT_MAX_LENGTH` 保持一致。
 *
 * 两个读取点：编辑页的超限横幅（提前拦住，否则自动保存会被服务端静默拒绝、状态条永远停在
 * 「仅本地待同步」）、文件导入的前置检查（#40：超限在导入时就拦下，不让用户白编辑一遍）。
 */
export const NOTE_CONTENT_MAX_LENGTH = 100000
